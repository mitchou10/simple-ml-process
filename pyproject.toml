[project]
name = "simple-ml-process"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.3",
    "pydantic>=2.11.9",
]

[dependency-groups]
datasets = [
    "kaggle>=1.7.4.5",
]
dev = [
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
    "ruff>=0.13.2",
]
images = [
    "pillow>=11.3.0",
]
model = [
    "torch>=2.8.0",
]
train = [
    "mlflow>=3.4.0",
    "seaborn>=0.13.2",
    "torchvision>=0.23.0",
]
